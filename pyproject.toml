[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "masterblaster-control"
version = "1.0.0"
description = "MasterBlaster-Control • Kali MCP Nexus v1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.7.0",
    "PyYAML>=6.0.1",
    "matplotlib>=3.8.0",
]
